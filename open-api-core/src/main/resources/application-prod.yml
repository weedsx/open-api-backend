# 线上配置文件
# @author <a href="https://github.com/weedsx">weeds</a>
# 
server:
  port: 8102
spring:
  # 数据库配置
  # todo 需替换配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://106.15.47.33:3306/open_api
    username: root
    password: llh112358
  # Redis 配置
  # todo 需替换配置
  redis:
    database: 3
    host: 106.15.47.33
    port: 6379
    timeout: 5000
    password: llh112358
  # Elasticsearch 配置
  # todo 需替换配置
#  elasticsearch:
#    uris: http://localhost:9200
#    username: root
#    password: 123456

# dubbo 配置
dubbo:
  application:
    name: open-api-core
    # 禁用QOS同一台机器可能会有端口冲突现象
    qos-enable: false
    qos-accept-foreign-ip: false
    enable-file-cache: false
  # 设置协议-协议由提供方指定消费方被动接受
  protocol:
    name: dubbo
    port: 20880
  registry:
    id: open-api-core
    address: nacos://106.15.47.33:8848

mybatis-plus:
  configuration:
    # 生产环境关闭日志
    log-impl: ''